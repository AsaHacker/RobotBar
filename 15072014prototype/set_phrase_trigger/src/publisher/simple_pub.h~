#include "ros/ros.h"
#include "std_msgs/String.h"
#include <fstream>

namespace simple_pub{

  class SimplePub{
public:
    SimplePub(ros::Nodehandle nh){

    }
    virtual ~SimplePub(){};
    void input_func();
private:
std::string 
  ros::Publisher input_pub;
  };
}
